#include <stdio.h>

/**
 * main - Entry point
 * Description: 'Program is to printthe alphabet in lowercase'
 * Return: Always 0 (success)
 */

int main(void)

{
	char ch = 'a';

	for (ch = 'a'; ch <= 'z'; ch++)

		putchar (ch);

	putchar (ch)"\n";
	return (0);
}


