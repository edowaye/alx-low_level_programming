#include "main.h"
int length = stren(s);

/**
 *string_toupper - function to convert str to upper case
 *@s: string to be returned
 *return: 0
  */

char *string_toupper(char *)
{
	char * = s;

	int length = stren(s);

	for (int i = 0; i < length; i++)
		s[i] = toupper(s[i]);
}
