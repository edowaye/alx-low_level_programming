#include "main.h"
int _strlen(char *s)

/**
  * *_strcat - Function prototype
  *
  *@dest: destination str
  *@src: source str
  *
  *Return:str
  */

int _strlen(char *s)
{
	int i;
	int count = 0;

	for (i = 0; s[i] != '\0'; x++)
		count +=1;

	return (count);
 }


char *_strcat(char *dest, char *src)
{

	char dest[] = x;
	char src[] = y;

	_strcat(dest, src);

	_putchar("%s\n", dest);

	return (0);
	}
