#include "main.h"
#include <stdio.h>

/**
  *_puts - Prototype function
  *
  *@str: Input to print
  *
  *Description: A function that prints a string,
  *followed by a new line, to stdout
  *
  *Return: A string
  */

void _puts(char *str)
{
	char *str[100];
	int i = 0;

	putchar(*s);

	while (*(str + i) != '\0')
	{
		putchar(*str + i);
		i++;
	}
	return (i);i
}
